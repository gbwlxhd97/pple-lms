import { IconProps } from '../types';
import UserSvg from '@/assets/icon/user.svg?react';
const UserIcon = ({ width, height }: IconProps) => {
  return <UserSvg width={width} height={height} />;
};

export default UserIcon;
