import { IconProps } from '../types';
import LogoutSvg from '@/assets/icon/logout.svg?react';
const LogoutIcon = ({ width, height }: IconProps) => {
  return <LogoutSvg width={width} height={height} />;
};

export default LogoutIcon;
