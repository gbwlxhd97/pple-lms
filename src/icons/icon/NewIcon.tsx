import { IconProps } from '../types';
import NewSvg from '@/assets/icon/new.svg?react';

const NewIcon = ({ width, height }: IconProps) => {
  return <NewSvg width={width} height={height} />;
};

export default NewIcon;
